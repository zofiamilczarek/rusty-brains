// to implement:
// - feedforward layer struct : an integrated weight + bias matrix, an activation function
// - model struct : a (list?) of objects with the trait can_backprop (this can be layers, transformer blocks, embedding layers)
// - activation function


